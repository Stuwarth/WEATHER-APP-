import React from 'react';
import { View, Text } from 'react-native';

export default function NativePrecipitationMap() {
  return (
    <View style={{ height: 180, borderRadius: 12, overflow: 'hidden', backgroundColor: 'rgba(0,0,0,0.15)', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#fff' }}>Mapa nativo no disponible en Web</Text>
    </View>
  );
}
